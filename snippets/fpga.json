{
    "read(addr, n)": {
        "prefix": "read",
        "body": "fpga.read(${1:addr}, ${2:n})",
        "description" : "Reads n number of bytes from the 16-bit address addr, and returns a bytes object."
    },
    "write(addr,bytes[])": {
        "prefix": "reset",
        "body": "fpga.write(${1:addr},${2:bytes})",
        "description" : "Writes all bytes from a given bytes object bytes[] to the 16-bit address addr"
    }
}
